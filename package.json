{
  "name": "phosphene",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Phosphene — closed-eye vision inside black frames. A WebGL2 installation with pad, glitch, and gated mic delay.",
  "homepage": "https://chrisloken.github.io/phosphene/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "publish:pages": "./scripts/publish-github-pages.sh"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vite": "^7.1.5"
  }
}
